# Phase 5: Frontend

## What it does
Provides a visual dashboard to monitor the data pipeline and view the generated weekly reports.

## Input File
- Calls API endpoints from Phase 4 (`/api/status`, `/api/report/latest`).

## Output File
- Visualizations in the browser.

## How to run
```bash
cd phase5_frontend
npm install
npm run dev
```
